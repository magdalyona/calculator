// ============== Operators

let a = 10
let b = 5
let c = 32

// c = c + a
// c = c - a
// c = c * a
// c = c / a

// можно писать так :

// c += a
// c -= a
// c *= a
// c /= a

// c = 4 ** 2  возведение в степь


// ==== Типы данных

const age = 29 // number номер
const name = "Magdalyona" // string строчка
const isProgrammer = true // boolean два значение применяет

let x // undedined без значения , существует но его нет, не обьявлено
// null отдельный тип данных
// // nan не число 
// console.log(typeof 1 / x)
// console.log(typeof NaN)

// ======      приоритеты 
const fullAge = 31
const birthYear = 1992
const currentYear = 2024
// < > (>) (<)
const isFullAge = currentYear - birthYear (>) fullAge

// console.log(isFullAge)


// что надо запомнить!!!!!!!!!!!! ->
const num1 = 42 // число
const num2 = "42" // строчка

console.log(num1 === num2) // двойное тройное равно сравнивает значения 